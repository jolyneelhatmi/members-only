extends layout

block content
  h1= title

  ul
    each post in post_list
      li
        p #{post.text}
        if user
          p #{post.author.username}
        else 
          p Anonymous
        

    else
      li There are no posts.
